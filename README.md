configuration is based on exvim: <https://github.com/jwu/exvim> with some customed settings

------------------------
####install:
    cp -r toolkit ~/.toolkit
    cp -r vim ~/.vim
    cp .vimrc ~/.vimrc
    cp .vimrc_ex ~/.vimrc_ex

